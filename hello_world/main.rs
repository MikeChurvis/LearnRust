// The main function is the universal entrypoint into all executables built with Rust.
fn main() {
    // This is a macro, as are all invokable things with names that end in '!'.
    // The distinction between macros and functions will be discussed in Chapter 19.
    println!("Hello, world!");  
}
